I have analyzed the current `index.html` and the assets in `web_material`. The current `index.html` is a template for "RoboHiMan" and needs to be completely updated for "INHerit-SG".

Here is the implementation plan:

1.  **Update Page Metadata & Header**:
    *   Set Title to **"INHerit-SG"**.
    *   Set Paper Title to **"INHerit-SG: Incremental Hierarchical Semantic Scene Graphs with Closed-Loop Verification"**.
    *   **Authors**: Since the paper text isn't accessible, I will add **placeholder author blocks** that you can fill in.
    *   **Links**:
        *   **Paper**: Link to `web_material/INHerit-SG_ Incremental Hierarchical Semantic Scene Graphs with Closed-Loop Verification.pdf`.
        *   **Code**: Placeholder link.
        *   **Video**: Link to the teaser video section.

2.  **Teaser Section**:
    *   Use `web_material/demo_video.mp4` as the main **Teaser Video** (autoplay, loop, muted).
    *   *Note*: For the `Teaser - 副本.pdf` and `pipeline_0131 - 副本.pdf`, web browsers don't display PDFs as images directly. I will add **placeholder `<img>` tags** pointing to `.png` versions and comment that you should convert these PDFs to images (e.g., `teaser.png`, `pipeline.png`).

3.  **Abstract Section**:
    *   Add a placeholder for the abstract text.

4.  **Interactive Visualization Section (New)**:
    *   Create a new section to embed `web_material/8_sgs.html` using an `<iframe>`. This will allow users to interact with the 3D scene graph directly on the page.

5.  **Results Section**:
    *   Create a **Video Grid/Carousel** displaying the HM3D results:
        *   `web_material/hm3d_01.mp4`
        *   `web_material/hm3d_02.mp4`
        *   `web_material/hm3d_03.mp4`
        *   `web_material/hm3d_04.mp4`
        *   `web_material/hm3d_05.mp4`
        *   `web_material/hm3d_06.mp4`

6.  **BibTeX Section**:
    *   Add a placeholder BibTeX block for the citation.

I will overwrite `index.html` with this new structure.
